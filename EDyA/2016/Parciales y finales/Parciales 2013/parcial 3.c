/*
	1
		a) Ordenar los archivos en funcion del tamaño de forama acendente.
	3 a)
		Tomando como pivote el primer elemento, la funcion quicksort intercambia los valores del arreglo
		de manera que los elementos que sean menores al pivote quede a la izquieda y los valores mayores
		al pivote quede al derecha del arreglo. El procedimiento lo ejecuta la funcion de particion
		que retorna el pivote.
	b) mmm no se si es una respuesta correcta: 
		Por ejemplo si la funcuion quicksort ordena los elementos de forma acendente, un arreglo
		para el peor caso de complejidad seria que los elementos esten ordenados de forma decendente.
*/
